Sets up the container for deployment to Hugging Face Spaces.

Dockerfile
FROM python:3.10-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

# Default command just runs validation or can be set to run a FastAPI wrapper if OpenEnv requires a server
CMD ["python", "inference.py"]
